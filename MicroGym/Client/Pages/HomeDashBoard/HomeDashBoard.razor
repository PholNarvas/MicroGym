@page "/"
@using BaseApplicationMethods
@using MicroGym.Shared.Model
@attribute [Authorize]
@layout MainLayout
@inherits BaseApplication

<PageTitle>Home - MicroGym</PageTitle>

<div class="dashboard">

    <!-- ═══ Header ═══ -->
    <div class="dash-header">
        <div>
            <h1 class="dash-title">@GetGreeting()@(currentUserName.Length > 0 ? $", {currentUserName}" : "")!</h1>
            <p class="dash-sub">@DateTime.Now.ToString("dddd, MMMM dd, yyyy") &nbsp;·&nbsp; Here's your gym at a glance.</p>
        </div>
    </div>

    <!-- ═══ KPI Cards ═══ -->
    <div class="stats-row home-kpi-row">
        @if (isLoading)
        {
            <div class="kpi-card kpi-loading"><div class="loader"></div></div>
            <div class="kpi-card kpi-loading"><div class="loader"></div></div>
            <div class="kpi-card kpi-loading"><div class="loader"></div></div>
            <div class="kpi-card kpi-loading"><div class="loader"></div></div>
        }
        else
        {
            <!-- Today's Check-ins -->
            <div class="kpi-card kpi-card--green">
                <div class="kpi-body">
                    <div class="kpi-label">Today's Check-ins</div>
                    <div class="kpi-value kpi-green">@todayCheckInCount</div>
                    <div class="kpi-sub">Members visited today</div>
                </div>
                <div class="kpi-icon kpi-icon-green">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>
                    </svg>
                </div>
            </div>

            <!-- Expiring This Week -->
            <div class="kpi-card @(expiringMembers.Any() ? "kpi-card--red" : "kpi-card--muted")">
                <div class="kpi-body">
                    <div class="kpi-label">Expiring This Week</div>
                    <div class="kpi-value @(expiringMembers.Any() ? "kpi-red" : "")">@expiringMembers.Count</div>
                    <div class="kpi-sub">Memberships expiring in 14 days</div>
                </div>
                <div class="kpi-icon @(expiringMembers.Any() ? "kpi-icon-red" : "kpi-icon-muted")">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
                    </svg>
                </div>
            </div>

            <!-- Total Registered -->
            <div class="kpi-card kpi-card--purple">
                <div class="kpi-body">
                    <div class="kpi-label">Total Members</div>
                    <div class="kpi-value kpi-purple">@totalMembers</div>
                    <div class="kpi-sub">All registered members</div>
                </div>
                <div class="kpi-icon kpi-icon-purple">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4"/>
                    </svg>
                </div>
            </div>

            <!-- Total Active -->
            <div class="kpi-card kpi-card--blue">
                <div class="kpi-body">
                    <div class="kpi-label">Total Active</div>
                    <div class="kpi-value kpi-blue">@totalActiveMembers</div>
                    <div class="kpi-sub">Active members across plans</div>
                </div>
                <div class="kpi-icon kpi-icon-blue">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/>
                    </svg>
                </div>
            </div>
        }
    </div>

    @if (!isLoading)
    {
        <!-- ═══ Quick Actions ═══ -->
        <div class="home-quick-actions">
            <button class="quick-action-btn quick-action-primary"
                    @onclick="OpenAddModal">
                <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4"/>
                </svg>
                Add Member
            </button>
            <button class="quick-action-btn"
                    @onclick='() => NavigationManager.NavigateTo("/admin-attendance")'>
                <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>
                </svg>
                Check In Member
            </button>
            <button class="quick-action-btn"
                    @onclick='() => NavigationManager.NavigateTo("/admin-revenue")'>
                <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
                </svg>
                View Revenue
            </button>
            <button class="quick-action-btn"
                    @onclick='() => NavigationManager.NavigateTo("/member-dashboard")'>
                <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
                </svg>
                Manage Members
            </button>
        </div>

        <!-- ═══ Expiring Soon Alert ═══ -->
        @if (expiringMembers.Any())
        {
            <div class="ant-card gym-table-card home-expiring-card">
                <div class="ant-card-body home-expiring-header">
                    <div style="display:flex;align-items:center;gap:0.5rem;">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" style="color:var(--danger);flex-shrink:0;">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
                        </svg>
                        <span class="dash-title home-expiring-title">Expiring This Week</span>
                    </div>
                    <span class="ant-tag ant-tag-red">@expiringMembers.Count total</span>
                </div>

                <!-- Search + Sort toolbar -->
                <div class="toolbar">
                    <span class="ant-input-affix-wrapper ant-input-affix-wrapper-lg gym-input search-input">
                        <span class="ant-input-prefix">&#128269;</span>
                        <input class="ant-input"
                               type="text"
                               placeholder="Search by name or email..."
                               value="@searchText"
                               @oninput="OnAlertSearchInput" />
                    </span>
                    <button class="home-sort-btn" @onclick="OnToggleSort" title="Sort by subscription expiry">
                        <svg xmlns="http://www.w3.org/2000/svg" width="13" height="13" fill="none"
                             viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.2">
                            @if (sortAscending)
                            {
                                <path stroke-linecap="round" stroke-linejoin="round" d="M3 4h13M3 8h9M3 12h5m10 4l-4-4m0 0l-4 4m4-4v12"/>
                            }
                            else
                            {
                                <path stroke-linecap="round" stroke-linejoin="round" d="M3 4h13M3 8h9M3 12h5m10-4l-4 4m0 0l-4-4m4 4V4"/>
                            }
                        </svg>
                        @(sortAscending ? "Asc" : "Desc")
                    </button>
                </div>

                <div class="ant-card-body home-table-body">
                    <table class="gym-table home-expiring-table">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Phone</th>
                                <th>Plan</th>
                                <th>Subscription Expiry</th>
                                <th>Annual Expiry</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var m in pagedExpiring)
                            {
                                var subDaysLeft    = m.ExpiryDate.HasValue
                                                     ? (m.ExpiryDate.Value.Date - DateTime.Today).Days
                                                     : (int?)null;
                                var annualDaysLeft = m.TierExpiryDate.HasValue
                                                     ? (m.TierExpiryDate.Value.Date - DateTime.Today).Days
                                                     : (int?)null;

                                <tr>
                                    <td>
                                        <div style="display:flex;align-items:center;gap:0.6rem;">
                                            <div class="member-avatar-sm">@m.FirstName[0]@m.LastName[0]</div>
                                            <div>
                                                <div class="cell-name">@m.FirstName @m.LastName</div>
                                                <div class="cell-muted" style="font-size:0.75rem;">@m.Email</div>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="cell-muted">@(m.Phone ?? "—")</td>
                                    <td class="cell-muted">@(m.MemberShipType ?? "—")</td>
                                    <td>
                                        @if (subDaysLeft.HasValue)
                                        {
                                            <span class="ant-tag @GetExpiryTagClass(subDaysLeft.Value)">
                                                @GetHomeExpiryTagText(subDaysLeft.Value)
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="cell-muted">—</span>
                                        }
                                    </td>
                                    <td>
                                        @if (annualDaysLeft.HasValue)
                                        {
                                            <span class="ant-tag @GetExpiryTagClass(annualDaysLeft.Value)">
                                                @GetHomeExpiryTagText(annualDaysLeft.Value)
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="cell-muted">—</span>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <PaginationControls CurrentPage="expiryPage"
                                        TotalPages="expiryTotalPages"
                                        TotalItems="displayedMembers.Count"
                                        PageSize="ExpiryPageSize"
                                        OnPageChanged="OnExpiryPageChange" />
                </div>
            </div>
        }
    }

</div>

<!-- ═══ Add Member Modal ═══ -->
<AddMemberModal IsOpen="showAddModal"
                OnClose="CloseAddModal"
                OnMemberAdded="OnMemberAdded" />
